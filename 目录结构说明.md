# 目录结构说明（ComfyUI-H3-Director）

> 本文件说明插件目录内各文件/文件夹的用途，便于维护与二次开发。
> 插件版本：v2.40.2 ｜ 许可证：MIT

## 一、目录总览

```
ComfyUI-H3-Director/
├── __init__.py            # 插件入口：注册节点、加载前端、挂载路由
├── studio_node.py         # 核心节点 H3DirectorStudio 实现（最大源码文件）
├── nodes.py               # 辅助节点（尾帧接力、分段合并等）
├── routes.py              # 后端 API 路由（界面与后端通信）
├── media_utils.py         # 媒体处理工具库（图片/视频/音频）
├── web/                   # 前端界面（导演台面板）
│   └── js/                #   前端脚本（8 个，见下文）
├── pyproject.toml         # 插件元数据与版本
├── requirements.txt       # Python 依赖清单
├── README.md              # 项目文档与完整更新日志
├── 安装说明.txt           # 中文安装与使用说明
├── LICENSE                # MIT 开源许可证
├── 导演台7.1.json         # ComfyUI 工作流示例（含 7.0 升级适配）
└── .gitignore             # Git 忽略规则
```

## 二、各文件详解

| 文件 | 大小 | 作用 |
|---|---|---|
| `__init__.py` | 513 B | **插件入口**。ComfyUI 加载插件时首先执行：注册全部节点、声明前端目录 `web/`、注册后端路由；任一步失败会输出提示但不阻断加载 |
| `studio_node.py` | 264 KB | **核心节点**。`H3DirectorStudio`（H3 漫剧导演台·一体节点）的全部逻辑：多段编排、参考素材编码、采样、解码、段视频/尾帧写盘、自动合并成片 |
| `nodes.py` | 15.8 KB | **辅助节点**。`H3DirectorTailFrame`（尾帧加载节点，段间画面接力）、`H3DirectorMerge`（按段合并成片）等小节点 |
| `routes.py` | 177 KB | **后端 API 路由**。导演台网页界面与 ComfyUI 后端的通信层：保存/读取设置、发起生成、查询进度与诊断等 |
| `media_utils.py` | 36 KB | **媒体工具库**。图片缩放/裁剪、视频抽帧、音频处理等通用函数，供核心节点调用 |
| `pyproject.toml` | 363 B | 插件元数据：名称 `comfyui-h3-director`、版本 `2.40.2`、许可证声明、依赖声明 |
| `requirements.txt` | 36 B | Python 依赖清单：`imageio-ffmpeg>=0.6.0`、`opencv-python`（其余依赖由 ComfyUI 提供） |
| `README.md` | 83 KB | 项目首页文档：功能介绍、安装步骤、完整使用说明与全部版本更新日志 |
| `安装说明.txt` | 25.9 KB | 中文安装/使用说明（与 README 互补，面向快速上手） |
| `LICENSE` | 1 KB | MIT 开源许可证（`Copyright (c) 2026 H3 Director contributors`） |
| `导演台7.1.json` | 208 KB | ComfyUI 工作流示例文件，可直接在 ComfyUI 中加载使用 |
| `.gitignore` | 27 B | Git 忽略规则：排除 Python 缓存 `__pycache__/`、`*.pyc` 与本地备份 `*.bak_*` |

## 三、前端脚本 web/js/（8 个）

| 文件 | 大小 | 作用 |
|---|---|---|
| `h3_studio.js` | 874 KB | **导演台主界面**：可视化时间轴、分段卡片、运行控制（最大前端文件） |
| `h3_guided_templates.js` | 441 KB | 引导式模板：分镜模板选择与向导流程 |
| `h3_script_tools.js` | 201 KB | 脚本工具：AI 剧情 / AI 分镜 / AI 精修等编辑功能 |
| `h3_second_sample.js` | 33 KB | 二采面板：3D latent 放大 + 低降噪修复采样 |
| `h3_templates.js` | 14 KB | 模板数据与逻辑 |
| `h3_create_projects.js` | 6.9 KB | 项目创建与初始化 |
| `h3_video_upscale.js` | 6.7 KB | 视频超分面板 |
| `h3_ui_interactions.js` | 3.2 KB | UI 交互辅助（拖拽、缩放等） |

## 四、本地环境文件（自动生成，不上传）

| 条目 | 说明 |
|---|---|
| `.git/` | Git 仓库元数据（由 `git init` 生成，管理版本历史） |
| `__pycache__/` | Python 字节码缓存（运行自动生成，可随时删除） |
| `studio_node.py.bak_*` | 本地备份文件（修改源码前手动备份，已被 `.gitignore` 排除） |
